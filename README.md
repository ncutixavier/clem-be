# clem-be
Company Leave &amp; Employee Management System

### Setup new project
`pip install "fastapi[all]" sqlalchemy psycopg2 alembic python-dotenv`

### Run the app
`uvicorn app.main:app --reload`
